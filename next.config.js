/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['s1.decofinder.com','s3.decofinder.com', 's6.decofinder.com', 's5.decofinder.com', 'www.decofinder.com'],
  },
}

module.exports = nextConfig
